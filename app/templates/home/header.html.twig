    
        <header>
            {% block logoContainer %}
            <section class="logoAndBtnContainer d-flex justify-content-between align-items-center px-3 py-2 bg-light">
                <a href="{{ path('app_home') }}">
                    <img src="{{ asset('pictures/logo.jpeg') }}" width="150px" alt="logo">
                </a>
            
                <div>
                    <button class="btn btn-outline-secondary">EN </button>
                    <button>Mode</button>



                    <div class="btnmode btn btn-outline-secondary">
                        <img
                            src="{{ asset('pictures/darkmode.png') }}"
                            alt="change light mode"
                            id = "lightMode"
                        />
                        <img
                            src="{{ asset('pictures/lightmode.png') }}"
                            alt="change light mode"
                            id = "darkMode"

                        />
                    </div>

                    
                </div>
        </section>
            {% endblock %}

            {% block menu %}
            <section class="navBarContainer">
                <section class="navBarContainer">
                    <nav class="custom-nav">
                        <!-- Navigation -->
                        <ul class="nav-links">

                            <li class="nav-item">
                                <a href="{{ path('app_members') }}">Le club</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('app_home') }}">École du bad</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('app_prices') }}">Tarifs</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('app_actuality') }}">Actualité</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('app_partners') }}">Partenaires</a>
                            </li>

                            <li class="nav-item">
                                <a href="{{ path('app_contact') }}">Contact</a>
                            </li>

                            {% if app.user %}
                                <li class="nav-item">
                                    <a href="{{ path('app_account') }}">Mon compte</a>
                                </li>
                                <li class="nav-item">
                                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    <a href="{{ path('app_login') }}">Connexion</a>
                                </li>
                            {% endif %}

                            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COACH') %}
                                <li class="nav-item">
                                    <a href="{{ path('app_admin_dashboard') }}">Administration</a>
                                </li>
                            {% endif %}
                        </ul>
                    </nav>
                </section>

            {% endblock %}

            {% block buttunsContainers %}
           <section class="hero">
                <div class="hero-buttons">
                    <a href="{{ path('app_schedules') }}">Voir les créneaux</a>
                    {% if app.user %} 
                        <a href="{{ path('app_account') }}">Mon compte</a>
                    {% else %} 
                        <a href="{{ path('app_register') }}">M'inscrire</a>
                    {% endif %}
                    <a href="{{ path('app_try') }}">Faire un essai</a>
                </div>
</section>
            {% endblock %}
        </header>
