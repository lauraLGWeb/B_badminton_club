{% extends 'base.html.twig' %}

{% block title %}Réinitialise ton mot de passe{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1> Réinitialise ton mot de passe</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div>
            <small>
                Entre ton adresse mail, et nous allons t'envoyer un lien pour réinitialiser ton mot de passe.
            </small>
        </div>

        <button class="btn btn-primary"> Envoyer le mail pour réinitialiser mon mot de passe</button>
    {{ form_end(requestForm) }}
{% endblock %}
